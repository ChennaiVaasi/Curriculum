import {Button} from '@/components/ui/button';import {Textarea} from '@/components/ui/textarea';
export function TaxonomyUploadPanel({text,setText,file,setFile,onClassify,loading,onExport}:{text:string;setText:(v:string)=>void;file:File|null;setFile:(f:File|null)=>void;onClassify:()=>void;loading:boolean;onExport:(f:'jsonl'|'csv')=>void}){return <div className="rounded-2xl border bg-white p-4 shadow-sm"><div className="mb-3 flex flex-wrap gap-3"><input type="file" accept=".pgn" onChange={e=>setFile(e.target.files?.[0]??null)} /><span className="text-sm text-stone-500">{file?.name}</span></div><Textarea className="min-h-40" placeholder="Paste PGN text…" value={text} onChange={e=>setText(e.target.value)} /><div className="mt-3 flex gap-2"><Button onClick={onClassify} disabled={loading}>{loading?'Classifying…':'Classify'}</Button><Button variant="outline" onClick={()=>onExport('jsonl')}>Export JSONL</Button><Button variant="outline" onClick={()=>onExport('csv')}>Export CSV</Button></div></div>}
