import type{TaxonomyRow}from'./types';
export function TaxonomyDetailDrawer({row,onClose}:{row:TaxonomyRow|null;onClose:()=>void}){if(!row)return null;return <div className="fixed inset-0 z-50 bg-black/30" onClick={onClose}><aside className="ml-auto h-full w-full max-w-xl overflow-y-auto bg-white p-6 shadow-xl" onClick={e=>e.stopPropagation()}><button className="float-right" onClick={onClose}>✕</button><h2 className="text-2xl font-semibold">Game {row.game_index}</h2><p className="text-stone-500">{row.white} vs {row.black} · {row.result}</p><Section title="Headers"><pre className="whitespace-pre-wrap text-xs">{JSON.stringify(row.headers??{},null,2)}</pre></Section><Section title="Opening classification"><p>{row.opening_family} / {row.opening_variation} / {row.opening_subvariation}</p></Section><Section title="Phase/domain"><p>{row.phase} · {row.domain}</p></Section>{[['Primary themes',row.primary_themes],['Micro-tags',row.micro_tags],['Structures',row.structures],['Material tags',row.material_tags]].map(([t,a]:any)=><Section key={t} title={t}><p>{a.join(' | ')}</p></Section>)}<Section title="Confidence"><p>{row.confidence.toFixed(2)}</p></Section><Section title="Raw PGN preview"><pre className="whitespace-pre-wrap text-xs">{row.raw_pgn_preview}</pre></Section></aside></div>}
function Section({title,children}:{title:string;children:React.ReactNode}){return <section className="mt-5"><h3 className="mb-2 font-semibold">{title}</h3>{children}</section>}
