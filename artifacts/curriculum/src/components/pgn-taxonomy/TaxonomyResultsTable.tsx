import type{TaxonomyRow}from'./types';
const join=(a:string[])=>a.join(' | ');
export function TaxonomyResultsTable({rows,onSelect,page,setPage}:{rows:TaxonomyRow[];onSelect:(r:TaxonomyRow)=>void;page:number;setPage:(n:number)=>void}){const size=50,total=Math.max(1,Math.ceil(rows.length/size)),shown=rows.slice((page-1)*size,page*size);return <div className="overflow-hidden rounded-2xl border bg-white"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-stone-100 text-xs"><tr>{['Game #','White','Black','Result','ECO','Opening family','Variation','Phase','Domain','Primary themes','Micro-tags','Structures','Material tags','Confidence'].map(h=><th className="whitespace-nowrap p-2" key={h}>{h}</th>)}</tr></thead><tbody>{shown.map(r=><tr key={r.game_index} className="cursor-pointer border-t hover:bg-amber-50" onClick={()=>onSelect(r)}><td className="p-2">{r.game_index}</td><td className="p-2">{r.white}</td><td className="p-2">{r.black}</td><td className="p-2">{r.result}</td><td className="p-2">{r.eco}</td><td className="p-2">{r.opening_family}</td><td className="p-2">{r.opening_variation}</td><td className="p-2">{r.phase}</td><td className="p-2">{r.domain}</td><td className="p-2">{join(r.primary_themes)}</td><td className="p-2">{join(r.micro_tags)}</td><td className="p-2">{join(r.structures)}</td><td className="p-2">{join(r.material_tags)}</td><td className="p-2">{r.confidence.toFixed(2)}</td></tr>)}</tbody></table></div><div className="flex items-center justify-between p-3 text-sm"><button disabled={page<=1} onClick={()=>setPage(page-1)}>Previous</button><span>Page {page} / {total}</span><button disabled={page>=total} onClick={()=>setPage(page+1)}>Next</button></div></div>}
